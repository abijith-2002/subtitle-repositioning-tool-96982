# Runtime dependencies for Subtitle Repositioning Backend
# Keep pins compatible and widely available on manylinux to avoid build failures.

# Web framework
fastapi~=0.110.0
starlette~=0.37.2

# ASGI server (standard pulls uvloop, httptools, websockets optionally where supported)
uvicorn[standard]~=0.27.1

# Data validation
pydantic~=2.6.0
pydantic-core~=2.18.0
typing_extensions>=4.8,<5

# File upload parsing
python-multipart>=0.0.6,<0.0.99

# Core processing libs
numpy==1.26.4
opencv-python-headless==4.8.0.76
# Use pure-Python SRT parser compatible with Python 3.12
srt>=3.5,<4

# OCR engine (optional, graceful fallback if not available)
# Relax pin to allow Python 3.12 compatible wheels; manylinux availability varies.
rapidocr-onnxruntime>=1.3.0,<2.0.0

# Optional (benign, used for envs if needed)
python-dotenv>=1.0,<2

# Linting (needed by CI linter script)
flake8>=6,<8

# Notes:
# - Removed dev/test tools (pytest, fastapi-cli) to reduce install surface.
# - Avoid pinning low-level libs (certifi, idna, httpx) unless used directly.
# - If you need GPU/OpenCV features, adjust opencv-python-headless version accordingly.
# - pysrt removed (incompatible with Python 3.12). Using 'srt' package instead.
